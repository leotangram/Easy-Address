import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  form: {
    display: "flex",
    flexDirection: "column",
    padding: 16,
  },
  input: {
    width: "100%",
  },
  button: {
    marginTop: 16,
  },
}));
