import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  error: {
    alignItems: "center",
    backgroundColor: "rgba(229, 26, 27, 0.5)",
    borderRadius: 8,
    display: "flex",
    flexDirection: "column",
    marginBottom: 16,
    width: "100%",
  },
}));
